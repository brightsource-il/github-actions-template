name: Pack & Push a Dotnet Nuget 

on:
  workflow_call:
    inputs:
      context_path:
        required: true
        type: string
      version:
        required: true
        type: string
permissions:
  contents: read
  
jobs:
  test:
    name: Service unit tests
    runs-on: ubuntu-latest    
    environment: ${{ inputs.environment }}
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 6.0.x
      - name: Restore dependencies
        run: |
          dotnet nuget add source ${{ secrets.BSII_NUGET_URL }} -n ArtifactoryV2   -u ${{ secrets.BSII_NUGET_USER }} -p ${{ secrets.BSII_NUGET_PASSWORD }} --store-password-in-clear-text
          dotnet restore ${{ inputs.context_path }}
      - name: Pack
        run: |
          cd ${{ inputs.context_path }}
          dotnet pack -c Release -o out -p:PackageVersion=${{ inputs.version }}
      - name: Push
        run: dotnet nuget push ${{ inputs.context_path }}/out/*.nupkg -Source Artifactory