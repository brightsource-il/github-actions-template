name: CI service

on:
  workflow_call:
    inputs:
      aws_ci_region:
        type: string
        default: "us-east-1"
      aws_ci_account:
        type: string
        default: "479886561928"
      enable-snyk:
        required: false
        type: boolean
        default: true
      enable-resharper:
        required: false
        type: boolean
        default: true
      enable-ecr:
        required: false
        type: boolean
        default: true
      enable-jfrog:
        required: false
        type: boolean
        default: false
      projects_args:
        default: "[]"
        required: true
        type: string

jobs:
  ci-service:
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(inputs.projects_args) }}
    uses: ./.github/workflows/ci-service.yaml
    secrets: inherit
    with:
        context_path: ${{ matrix.context_path }}
        solution_file: ${{ matrix.solution_file }}
        dockerfile_path: ${{ matrix.dockerfile_path }}
        build-args: ${{ matrix.build-args }}
        service_name: ${{ matrix.service_name }}